# SQL Server Database Configuration (LEGACY - No se usa, el sistema usa ChromaDB)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=RAG_Database
DB_USER=sa
DB_PASSWORD=YourPassword123

# LLM API Configuration (Choose one or both)
# Groq API (10x faster, 14,400 requests/day free) - RECOMENDADO
GROQ_API_KEY=your_groq_api_key_here

# DeepSeek API (slower but good quality)
DEEPSEEK_API_KEY=your_deepseek_api_key_here
